I"o<h2 id="name-속성name-attribute"><strong>Name</strong> 속성(Name Attribute)</h2>

<p>Valid only on <code class="language-plaintext highlighter-rouge">&lt;a&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;iframe&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;map&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;input&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;select&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;textarea&gt;</code></p>

<p><strong>Name</strong> does not have to be unique, and can be used to group elements together such as radio buttons &amp; checkboxes</p>

<hr />

<h2 id="id-속성id-attribute"><strong>ID</strong> 속성(ID Attribute)</h2>

<p>Valid on <strong>any element except</strong> <code class="language-plaintext highlighter-rouge">&lt;base&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;html&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;head&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;meta&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;param&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;script&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;style&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;title&gt;</code></p>

<p><strong>(Important)</strong> The name attribute is <strong>used in the HTTP request</strong> sent by your browser to the server as a variable name associated with the data contained in the value attribute.</p>

<p><strong>Each Id should be unique</strong> in the page as rendered in the browser, which may or may not be all in the same file</p>

<p>Is referenced in <strong>CSS</strong> or <strong>URL</strong> with <code class="language-plaintext highlighter-rouge">#</code> sign</p>

<p>Is referenced in <strong>JS</strong> with <code class="language-plaintext highlighter-rouge">getElementById()</code></p>
:ET